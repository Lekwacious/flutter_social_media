import 'package:flutter/material.dart';
class Notiication extends StatelessWidget {
  const Notiication({super.key});

  @override
  Widget build(BuildContext context) {
    return  Container(
      color: Colors.blue,

    );
  }
}
